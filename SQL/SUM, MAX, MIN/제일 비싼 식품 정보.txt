SELECT *
FROM FOOD_PRODUCT
ORDER BY PRICE DESC
LIMIT 1;

SELECT *
FROM FOOD_PRODUCT
WHERE PRICE = (SELECT MAX(PRICE)
                FROM FOOD_PRODUCT
              )
-- Mysql, Oracle 모두 가능 코드
-- Oracle은 LIMIT 사용 안됨