SELECT COUNT(*) AS FISH_COUNT
FROM FISH_INFO I JOIN FISH_NAME_INFO N ON I.FISH_TYPE = N.FISH_TYPE
WHERE N.FISH_NAME = 'BASS' OR N.FISH_NAME = 'SNAPPER';

-- 두 테이블 조인 후 조건절에 FISH_NAME 칼럼을 사용해야 함
-- FISH_TYPE의 값은 바뀔 수 있기 때문