SELECT E1.ID, COUNT(E2.PARENT_ID) AS CHILD_COUNT
FROM ECOLI_DATA E1 LEFT JOIN ECOLI_DATA E2 ON E1.ID = E2.PARENT_ID
GROUP BY E1.ID
ORDER BY E1.ID

-- 한 테이블을 LEFT JOIN 수행(자식이 없는 경우 NULL값이 들어감)
-- 개체와 자식 사이의 관계를 연결해주는 것